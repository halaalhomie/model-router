# Test-only dependencies. Kept separate from requirements.txt so the runtime
# image we build in Phase 10 does not ship a test runner.
-r requirements.txt
pytest>=8.0.0,<9.0.0
# fastapi.testclient.TestClient needs plain httpx (not httpx2, which
# google-genai uses) -- it happened to already be present transitively in
# this venv, but pinning it explicitly means a fresh install doesn't
# silently depend on some other package continuing to pull it in.
httpx>=0.27.0,<1.0.0
# Lint. Configured in setup.cfg -- without a declared line length, editors
# fall back to 79 and flag most of this codebase as a style violation.
flake8>=7.0.0,<8.0.0
